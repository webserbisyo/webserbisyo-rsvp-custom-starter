import { EmptyState } from "@/components/ui/EmptyState";

export default function NotFoundPage() {
  return <EmptyState title="Page not found" message="The requested event page could not be found." />;
}
