# ReactBits-Style Components In Client Clones

ReactBits-style or similar generated components must live inside `src/client/libs/reactbits/` or `src/client/components/`.

Rules:

- do not paste generated components into protected platform files
- audit accessibility, mobile responsiveness, and performance
- do not add routes or RSVP submission behavior
- use real platform data, not hardcoded event content
