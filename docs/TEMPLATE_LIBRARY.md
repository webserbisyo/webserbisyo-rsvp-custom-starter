# Template Library

Current template:

- `wedding-custom-starter-v1`

This template should start from the same general section/card rhythm as the main platform wedding public/account preview renderer. Client themes and custom UI libraries belong in cloned client repos after baseline validation.

Future templates should keep the same public API and section registry boundary unless the platform contract changes.
