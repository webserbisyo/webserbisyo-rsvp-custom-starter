Placeholder images are intentionally not bundled yet.

This starter can use CSS treatments and sample data in design mode. Add client-approved assets in per-client repositories only when they are supplied as public-safe content.
